<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.kaplan.pdma.brandmaster.MainActivity"
    android:background="@drawable/background">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/imageView"
        android:src="@drawable/logo"
        android:layout_marginBottom="172dp"
        android:layout_above="@+id/bgm_switch"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Play"
        android:id="@+id/play_button"
        android:layout_gravity="center"
        android:layout_above="@+id/bgm_switch"
        android:layout_marginBottom="140dp"
        android:layout_alignStart="@+id/high_button"
        android:textColorHighlight="@color/accent_material_light"
        android:background="@drawable/round_button_corner"
        android:layout_alignEnd="@+id/high_button" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="High Scores"
        android:id="@+id/high_button"
        android:layout_gravity="center"
        android:layout_alignTop="@+id/play_button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="65dp"
        android:background="@drawable/round_button_corner" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Settings"
        android:id="@+id/setting_button"
        android:layout_gravity="center"
        android:background="@drawable/round_button_corner"
        android:layout_below="@+id/high_button"
        android:layout_alignEnd="@+id/high_button"
        android:layout_alignStart="@+id/high_button"
        android:layout_marginTop="15dp" />

    <Switch
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="BGM"
        android:id="@+id/bgm_switch"
        android:checked="true"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true" />

</RelativeLayout>
